# Development tools

- `ai/` contains offline pose and joint-angle analysis utilities.
- `diagnostics/` contains manual camera and dataset inspection scripts.
- `extract_video_contact_sheets.py` creates contact sheets used to review
  exercise reference videos.

Run tools from the repository root so their relative asset paths resolve
correctly.
